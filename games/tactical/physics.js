import RAPIER from 'rapier';
export async function createPhysics(){await RAPIER.init();const world=new RAPIER.World({x:0,y:-18,z:0});const floor=world.createCollider(RAPIER.ColliderDesc.cuboid(40,.4,40).setTranslation(0,-.4,0));const bounds=[[-40,2,0,.5,2,40],[40,2,0,.5,2,40],[0,2,-40,40,2,.5],[0,2,40,40,2,.5]];for(const [x,y,z,hx,hy,hz] of bounds)world.createCollider(RAPIER.ColliderDesc.cuboid(hx,hy,hz).setTranslation(x,y,z));const body=world.createRigidBody(RAPIER.RigidBodyDesc.kinematicPositionBased().setTranslation(0,1,20));const collider=world.createCollider(RAPIER.ColliderDesc.capsule(.55,.35).setTranslation(0,0,0),body);return{world,body,collider,step(dt){world.timestep=dt;world.step()},setPosition(x,y,z){body.setNextKinematicTranslation({x,y,z})},position(){return body.translation()}}}